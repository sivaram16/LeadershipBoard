query CalculateStarOfWeek($startDate:String!,$endDate:String!,$year:Int!,$section:String!){
  calculateStarOfWeek(startDate:$startDate,endDate:$endDate,year:$year,section:$section){
    pdf
    errors{
      errorCode
      message
      path
    }
  }
}